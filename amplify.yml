version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - rm -rf node_modules
        - pnpm install --ignore-scripts
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
